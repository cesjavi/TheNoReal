# TheNoReal

## Migraciones y pruebas

Ejecuta los siguientes comandos para instalar dependencias, aplicar migraciones y correr las pruebas:

```bash
npm install
npx prisma migrate dev --name add_story_config
npm test
```
